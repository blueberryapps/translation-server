<% @releases.each do |release| %>
# LOCALE:   <%= release.locale %>
# VERSION:  <%= release.version %>
# RELEASED: <%= release.created_at %>

<%= release.yaml.html_safe.split("\n")[1..-1].join("\n") %>

<% end %>
